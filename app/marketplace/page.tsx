import Header from '@/components/Header';import {products} from '@/lib/data';import Link from 'next/link';
export default function Marketplace(){return <><Header/><main className="wrap section"><div className="marketTop"><div className="eyebrow">AFRILYNQ MARKETPLACE</div><h1 style={{fontSize:52,letterSpacing:'-.05em',margin:'10px 0'}}>Agricultural supply, organised.</h1><p className="muted" style={{maxWidth:720,lineHeight:1.8}}>Browse commercial products from suppliers across African markets. Compare origin, MOQ and supplier information before you enquire.</p></div><div className="searchIn" style={{padding:'15px 0'}}><input className="input" style={{gridColumn:'1 / span 2'}} placeholder="Search maize, cashew, ginger..."/><Link href="/marketplace" className="btn dark">Search</Link></div><div className="productGrid marketGrid" style={{marginTop:22}}>{products.map(p=><article className="product" key={p.name}><div className="photo"><img src={p.image} alt={p.name}/></div><div className="body"><div className="eyebrow" style={{fontSize:9}}>VERIFIED SUPPLIER</div><h3>{p.name}</h3><div className="meta">{p.detail}</div><div className="facts"><span>Origin <b>{p.origin}</b></span><span>MOQ <b>{p.moq}</b></span></div><div className="rule"/><div className="meta">Supplier</div><strong>{p.supplier}</strong></div></article>)}</div></main></>}
